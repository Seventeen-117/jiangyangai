service: deepSearch-service
name: "/api/calculation/status/{requestId}"
request:
  method: GET
  url: ${TARGET_BASE_URL:http://localhost:8080}/api/calculation/status/{requestId}
  headers:
    Content-Type: application/json
    X-Gateway-Source: test-service
  body: {}
expect:
  status: 200
