service: signature-service
name: "/api/security/rate-limit/{clientId}"
request:
  method: GET
  url: ${TARGET_BASE_URL:http://localhost:8080}/api/security/rate-limit/{clientId}
  headers:
    Content-Type: application/json
    X-Gateway-Source: test-service
  body: {}
expect:
  status: 200
